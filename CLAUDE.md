@AGENTS.md
# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際に必ず参照する「プロジェクト説明書」です。
コードを書く前に、必ずこの内容に従ってください。

## プロジェクト概要

農業日誌PWA（個人開発 → 将来的に家族・従業員へ展開予定）。
圃場（10〜30件）ごとの作業記録、農薬・肥料の使用履歴、写真添付、環境データを記録し、
農薬取締法に基づく使用履歴の報告（PDF/Excel出力）に対応することが最重要要件。
前身プロジェクトは圃場管理簿（同一技術スタック）。

## 技術スタック

- フロントエンド: Next.js (App Router) / TypeScript
- ホスティング: Vercel（Hobby プラン、非収益化のため）
- バックエンド/DB: Supabase（東京リージョン）
- 認証: Supabase Auth（メール+パスワード）
- オフライン: IndexedDB + Supabase 同期（Last-Write-Win + 競合時は警告表示。PowerSyncは不採用）
- バージョン管理: GitHub

## コーディング規約

- TypeScript は strict モード。`any` は原則禁止、やむを得ない場合は理由をコメントで明記。
- コンポーネントは関数コンポーネント + Hooks。
- Server Component / Client Component の使い分けを明示し、不要な `"use client"` を避ける。
- Supabase クライアントは `lib/supabase` 以下に集約し、直接コンポーネントから初期化しない。
- 環境変数は `.env.local` を使用し、絶対にコミットしない（`.gitignore` 確認必須）。

## デザイン指針（UI を書くときは必ず従う）

方向性は「現場ツール（カミナシ / ANDPAD 系）× 高密度 SaaS（Linear 系）」のハイブリッド。
屋外・手袋・片手操作に耐えることと、1画面あたりの情報量を両立させる。
インドネシア人スタッフも使うため、意味はアイコン・色・数字で伝え、日本語の読解に依存させない。

画面デザインの一次資料（全タブのモック・トークン一覧・デスクトップ骨格）:
https://claude.ai/code/artifact/3a55d999-dc80-47f4-bd7b-06a0f3ff1ee1

### 3原則

1. **影を使わない。** 面の区別は 1px の罫線と背景色だけでつける。`--shadow-card` は廃止し、
   `.surface-card` からも外す。影が残るのはボトムシート（`--shadow-elevated`）だけ。
2. **色は「状態」と「今すぐ押すもの」にだけ使う。** 装飾に色を使わない。
   1画面にアクセント塗りの主ボタンは 1 つだけ。
3. **囲う理由があるときだけ囲う。** 同じ種類の羅列は全幅リスト＋1px 罫線。
   カードで囲うのは「別グループだと示す必要があるとき」だけ。

### 色トークン

`app/globals.css` のトークン名は変えず、値だけ差し替える。
既定アクセントはオレンジ（下表）。UI を書くときはハードコードせず必ず `--accent` 系を参照する。

アクセントは設定画面（表示セクション）から **オレンジ / ブルー / グリーン** の3択で切り替えられる
（`AccentPicker`。この端末だけの設定で `localStorage.accent` に保存、`<html data-accent>` で反映、
初期値は `layout.tsx` のインラインスクリプトが復元）。上書きされるのは accent 系トークンのみで、
状態色（success / warning / danger）と面の色は共通。ブルーは Apple 風の `#0071e3` 系ではなく
落ち着いた濃紺（ライト `#1E51C4` / ダーク `#6AA5F5`）を使う。

実装上の注意: `[data-theme="dark"][data-accent="blue"]` のような複合セレクタは
Next 16 の Lightning CSS が `[data-theme=...]` 側を落として潰す。そのため globals.css では
`[data-accent="blue"]` 側で light/dark 両方の生値（`--accent-l` / `--accent-d` …）を定義し、
テーマ側の単一セレクタで `--accent: var(--accent-l, <オレンジ>)` のように選ぶ二段構えにしている。
**accent 系を複合セレクタで書き足さないこと。**

ライト（既定・オレンジ）:

```
--background          #F5F4F1
--surface             #FFFFFF
--surface-secondary   #F0EEE9
--foreground          #14130F
--foreground-secondary #6E6A5E
--foreground-tertiary #8E8A7D
--separator           #EDEAE3
--separator-strong    #CFCBC0
--accent              #C2410C   --accent-hover #9A3412   --accent-bg #FDECE2
--success #256E36  --success-bg #E7F2E9
--warning #A85B00  --warning-bg #FDF3E3
--danger  #B42318  --danger-bg  #FBE9E7
```

ダーク:

```
--background          #101215
--surface             #171A1F
--surface-secondary   #1E2126
--foreground          #ECEEF1
--foreground-secondary #8B929C
--foreground-tertiary #7B828C
--separator           #1E2126
--separator-strong    #343941
--accent              #FF7A2F   --accent-bg #2C1A0E
--success #4ED07B  --warning #E8B04A  --danger #F27167
```

### 余白（4px グリッド）

使う値は **4 / 8 / 12 / 14 / 16 / 18 / 24 / 40** だけ。ここに無い数字を書かない。

| 値 | 用途 |
| --- | --- |
| 4 | アイコンと文字の隙間、バッジの内側 |
| 8 | チップ・ボタン同士、見出し→中身 |
| 12 | リスト行の左右パディング、行内の要素間 |
| 14 | カード内の左右パディング |
| 16 | **画面の左右パディング（モバイル）**、カード間 |
| 18 | 設定などのセクション間（モバイル） |
| 24 | ブロック間（デスクトップ） |
| 40 | **画面の左右パディング（md 以上）** |

- 並べるものは必ず `display:flex` / `grid` ＋ `gap`。個別 margin や空白文字で間隔を作らない。
- 本文の最大幅は `max-w-5xl`（1024px）中央寄せ。サイドバーは 256px 固定。

### タイポグラフィ

本文フォントは **IBM Plex Sans JP**（フォールバック: Hiragino Kaku Gothic ProN → system-ui）。
この書体は **最大ウェイトが 700**（900 は無い）。屋外・低解像度端末で太字が潰れにくいことを優先した選定。
数字を出す要素には必ず `font-variant-numeric: tabular-nums`。**11px 未満は使わない。**

| 用途 | サイズ / 太さ |
| --- | --- |
| 数値（KPI・集計） | 22 / 600 / letter-spacing -0.01em |
| 画面タイトル | 17 / 700 |
| 行タイトル | 15 / 700 |
| テーブル本文 | 14 / 400（強調セルのみ 700） |
| 副情報 | 12 / 400 / `--foreground-tertiary` |
| セクション見出し | 12 / 700 / letter-spacing .06em / `--foreground-secondary` |
| テーブル列見出し | 10-11 / 700 / letter-spacing .05em |

### 高さ・角丸・タップ領域

押せるものは **44px 以上**。主要操作は親指が届く画面下側に置く。

| 部品 | 高さ |
| --- | --- |
| アプリヘッダー | 48 |
| 主アクション（アクセント塗り） | 56 |
| 入力・セレクト | 48 |
| 選択チップ | 44 |
| リスト行・テーブル行 | 44 |
| サブタブ（ピル） | 38 ＋ 上下 8 |
| 下部タブバー | 56 ＋ safe-area |

角丸: 面（カード・テーブル）10 / ボタン・入力・チップ 8 / バッジ 4 /
ボトムシート上端 20 / サブタブ・トグルはピル（999）。**16px の角丸は使わない。**

### 各タブのページの型

新しい画面もこの型から外れないこと。

- **ホーム** — ヘッダー → 週ストリップ → 主アクション → KPI 3枚 → 本日のテーブル。
  1画面で完結させ、スクロールさせない。
- **収穫** — サブタブ（圃場ごと / 履歴）→ 圃場カード。累計を大きく出し、行内に 44px の追加ボタン。
- **管理** — 日付送り（44px の ◀ ▶）→ 作物ごとのブロック → チェック行。
  右肩に「2/3」の分数。完了行は打ち消し線＋グレー。
- **実績** — 登録はステップ式（横スワイプ）＋画面下に確定バー（現在の入力内容の要約と、
  足りない項目を赤字で名指し）。集計は 期間ピル → 合計 → 内訳テーブル。
  サブタブは画面下（親指が届く位置）に置く。
- **設定** — セクション見出し（12/900 muted）＋説明1行 → 白カードに行を積む。
  行は「タイトル 15/700・説明 12・右に操作部」。
  **細かい編集はページ遷移させず、ボトムシートで開く。** 下端に「削除 / 保存する」を固定。

### アイコン

`components/icons.tsx` の規約を維持する: 24px グリッド / `fill:none` /
`stroke:currentColor` / `stroke-width:1.7`（選択中のみ 2.0）/ 丸いキャップ。

### やらないこと

- 絵文字を UI に使わない。
- Apple 風の青（`#0071e3` 系）、16px 角丸、カードの影を新規に足さない。
  アクセントのブルーは設定で選べるが、上記の落ち着いた濃紺を使い、明るい `#0071e3` 系にはしない。
- 入力欄のフォントサイズを 16px 未満にしない（iOS が自動ズームするため）。
- グラフ（収量推移など）の色・軸・凡例はまだ決めていない。作る前に必ず確認する。

## 法令・データの正確性に関する注意（最重要）

- 農薬使用履歴・成分・使用基準に関するロジックは、憶測で実装せず、必ずユーザーに確認するか
  マスタデータ（登録済みの農薬・肥料リスト）を参照すること。
- 出力する報告書（PDF/Excel）のフォーマットや必須項目を変更する場合は、必ず事前に確認を取ること。

## Git運用ルール

- コミットメールアドレスはリポジトリローカルの `git config user.email` で
  GitHub の noreply エイリアスまたは仕事用メールを設定する（個人メールは使わない）。
- 会社PCへ移行する際は `git clone` 後、`.env.local` を再設定する運用を前提とする。
- **main への push は、指示があったときだけ行う。**
  作業が一区切りついてもローカルのコミットまでにとどめる。
  push・main へのマージは「push して」「main に入れて」と言われてから行う。

## 作業時の指示

- 大きな変更（DBスキーマ変更、認証まわり、同期ロジック）の前には設計方針を提示し、承認を得てから実装する。
- 小さな修正（UIの微調整、typo、既存関数の軽微なリファクタ）は確認なしで進めてよい。
- 実装後は必ず `npm run build` と型チェックが通ることを確認する。
- わからない業務仕様（農薬の使用基準など）は勝手に推測せず質問する。

## ディレクトリ構成（想定）

- `app/` - Next.js App Router のページ・レイアウト
- `components/` - UIコンポーネント
- `lib/supabase/` - Supabase クライアント・型定義
- `lib/offline/` - IndexedDB 同期ロジック
- `types/` - 共通の型定義